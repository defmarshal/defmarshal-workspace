# 2026-03-11 — Meta-agent Summary & Telegram Fixes

## Morning/Afternoon
- User reported not receiving meta-agent Telegram summaries.
- Investigated: found multiple attempts to run meta-agent via cron/screen/nohup loops.
- Discovered `agents/meta-agent.sh` had become empty (0 bytes) despite size showing 23294 bytes via stat — weird filesystem/script state issue. Restored from git commit `b788d2b8`.
- Meta-agent itself runs and spawns agents (content, archive, notifier, archiver-manager).
- Rate limit handling in place: cooldown lock working.

## Summary Delivery Problem
- Original meta-agent built-in summary was not integrated correctly.
- Created `agents/summary-sender.sh` to read `memory/meta-agent.log` and send Telegram digest.
- Setup user systemd timer `meta-summary.timer` to run hourly.
- Manual test of summary-sender showed "No recent meta-agent activity" because log was empty due to meta-agent script state.

## Telegram Bot Token Issue
- OpenClaw config had old/invalid bot token (`8549515410:AAFDfNrj3yrCCaPAOQHqED-tYkaVsQvvQ9M`).
- User provided new token: `8683142423:AAHCOuZRvr0JLSGg1YJWX4zzvemvkD9Hq1Q`.
- Updated `openclaw.json` with new token and added destinations entry for user ID 952170974.
- Verified bot works via direct Telegram API (`getMe` and `sendMessage`).
- Tested `openclaw message send` but CLI flags incorrect; will use summary-sender instead.

## File Stability Anomaly
- Noticed newly created files (scripts) appearing empty after creation, though `ls` shows non-zero size.
- Suspect some cleanup or git reset process. Need to investigate:
  - Any cron jobs reverting agents directory?
  - Workspace-builder resetting to git HEAD?
  - File system issues (overlayfs, tmpfs)?
- This threatens automation reliability.

## Action Items (Pending)
- Verify Telegram delivery of manual test sent via direct API (should have arrived).
- Fix file stability issue: find what's wiping newly created files.
- Ensure summary-sender can actually read meta-agent.log (which should contain entries after meta-agent runs).
- Ensure meta-agent continues running hourly.
- Consider making summary-sender use direct Telegram API to avoid OpenClaw CLI dependency.
- Add monitoring to detect when meta-agent.log stops growing.

## Notes
- User wants hourly summaries.
- Meta-agent runs every 2 hours via system crontab entry.
- Current crontab shows three similar entries (duplicate?):
  ```
  0 * * * * cd /home/ubuntu/.openclaw/workspace && META_AGENT_SEND_SUMMARY=1 ./agents/meta-agent.sh --once >> memory/meta-agent.log 2>&1
  ```
  Possibly added multiple times; should consolidate.
